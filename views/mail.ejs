<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Email Form with Bootstrap</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      background-color: #f8f9fa;
      padding: 20px;
    }

    form {
      max-width: 500px;
      margin: auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    label {
      margin-top: 10px;
      font-weight: bold;
    }

    button {
      margin-top: 20px;
    }
  </style>
</head>
<body style="background-image: linear-gradient(-90deg, #ff5858 0%, #f09819 100%);">

<div class="container" >
  <div class="text-center mb-4">
    <h1 class="mt-5">Email Form</h1>
    <p class="lead">Fill out the form below to send an email.</p>
  </div>

  <form action="/employerlogin/submitmail?category=<%= Category %>&job=<%= Job %>&company=<%= company %>" method="post" enctype="multipart/form-data">
    <span id="cat" style="display: none;"><%= Category %></span>  <span id="job" style="display: none;"><%= Job %></span><span id="company" style="display: none;"><%= company %></span>

    <div class="mb-3">
      <label for="to" class="form-label">To:</label>
      <input type="email" class="form-control" name="to" value="<%=to%>" readonly>
    </div>

    <div class="mb-3">
      <label for="subject" class="form-label">Subject:</label>
      <input type="text" class="form-control" name="subject" required>
    </div>

    <div class="mb-3">
      <label for="body" class="form-label">Body:</label>
      <textarea class="form-control" name="body" rows="4" required></textarea>
    </div>

    <div class="mb-3">
      <label for="attachment" class="form-label">Attachment:</label>
      <input type="file" class="form-control" name="attachment">
    </div>

    <button type="submit" class="btn btn-primary">Send Email</button>
  </form>
</div>

<!-- Bootstrap JS and Popper.js (optional) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>



